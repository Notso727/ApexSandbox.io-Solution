public static Integer findLargest(List<Integer> nums) {
    /* Implement the method findLargest that takes as input a non-empty list of Integers nums, and returns the
    largest Integer in the list.
     */

    //To find the largest integer, we need to sort ascending and then use size to get the last integer in the list
    nums.sort();
    Integer x = nums.size();
    return nums.get(x);
    
}