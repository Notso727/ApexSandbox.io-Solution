public Set<Integer> setUnion(Set<Integer> set1, Set<Integer> set2) {
    /* A union between two sets A and B is a set that contains all elements from A and B. For example, the unions
    of sets {1, 5, 10} and {1, 3, 5} is {1, 3, 5, 10}.

    Implement the method setUnion that takes as input two sets of integers set1 and set2 and returns the union of
    the two sets. The method should not modify the original sets.
     */

    //Use the 'addAll' method to conduct the union of the sets.

    Set<Integer> set3 = new Set<Integer>();
    set3.addAll(set1);
    set3.addAll(set2);

    return set3;

}
