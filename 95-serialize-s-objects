public String getAccountsInJSONFormat(List<Account> accounts){
    /* Implement the method getAccountsInJSONFormat(), a list of accounts and returns a list of accounts in
    string JSON format.
     */

    String jsonAccount = JSON.serialize(accounts);
    
    return jsonAccount;
    
}