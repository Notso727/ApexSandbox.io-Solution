public Boolean isTypeAccount(sObject record)
{
    /* Implement the method isTypeAccount(), which accepts a sObject as input and returns a true if type of input is
    Account object else it should return as false.
     */

    // Create a boolean variable
    Boolean accountTrue = false;

    //Check that List is not null first
    if(record == null) return false;

    if (record instanceof Account) accountTrue = true;

    return accountTrue;

}