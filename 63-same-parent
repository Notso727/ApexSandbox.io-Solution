public Boolean sameParent(Contact c, Opportunity opp) {
    /*Implement the method sameParent that takes as input an opportunity opp and a contact c, and returns true if both the 
    opportunity and contact have the same parent account.
    */

    //Check for null values first
    //Then check if the Contact and Opportunity have the AccountId value
    if (c.AccountId != null && opp.AccountId != null) {
        return(c.AccountId == opp.AccountId);       
    }

}