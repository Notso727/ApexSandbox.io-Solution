public String reverseWordsInASentence(String sentence){

//Reverse the sentence
//Create a variable to hold that.
    String reversedSentence = sentence.reverse();
    String newSentence;

//Create a list to hold the words
    List<String> word_list = reversedSentence.split(' ');

    //Create a Loop on List that will reverse the words

    List<String> final_list = new List<String>();
    for(String word : word_list) {
        final_list.add(word.reverse());
    }

//Now create a loop to assemble them.
    newSentence = final_list.toString();
    return newSentence;
}