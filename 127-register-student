public String registerStudent(String name, String phone, String email) {
    /*The method registerStudent takes as input strings name, phone and email, and returns a string. Implement the
    method to insert an apxio__Student__c record with the Name, apxio__Phone__c and apxio__Email__c fields filled out,
    and return the autogenerated apxio__Registration_Number__c of the new record.
     */

    //Create a new sObject of apxio__Course__c with the required fields
    apxio__Student__c newStudent = new apxio__Student__c();

   //Implement the newStudent with the inputs for name, phone and email
    newStudent.name = name;
    newStudent.apxio__Phone__c = phone;
    newStudent.apxio__Email__c = email;

    //Insert the new course
    insert newStudent;


    //Now run a SQOL to pull the registration Number
    apxio__Student__c studentToGet = [SELECT Id, apxio__Registration_Number__c
                                        FROM apxio__Student__c
                                       WHERE Id = :newStudent.Id];

    //Return Id
    return studentToGet.apxio__Registration_Number__c;

}