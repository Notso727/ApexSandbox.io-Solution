public static List<Integer> squareList(List<Integer> nums){
    /*
    Given a list of integers sorted in ascending order return a list of integers that contains the square of each
    value in the input list sorted in ascending order.
     */

    //Create a list to collect the values
    List<Integer> squaredNums = List<Integer>;

    //Iterate over the Nums List and square each value
    /*for (Integer n : nums){
        n = (n * n);
        squaredNums.add(n);
    }
    */
    //Sort the new list
    squaredNums.sort();

    return null;
}